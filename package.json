{
  "name": "komorni-orchestr-web",
  "version": "1.0.0",
  "description": "Web pro komorní orchestr",
  "scripts": {
    "build:css": "postcss src/css/main.css -o src/css/main.min.css",
    "build": "npm run build:css && eleventy",
    "dev": "npm run build:css && eleventy --serve",
    "watch": "npm run build:css && eleventy --watch",
    "css:watch": "postcss src/css/main.css -o src/css/main.min.css --watch"
  },
  "keywords": ["11ty", "eleventy", "static-site"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.2.0",
    "tailwindcss": "^3.4.1",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.33",
    "postcss-cli": "^11.0.0",
    "markdown-it": "^13.0.2"
  }
}

